<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/camera_screen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:keepScreenOn="true"
    tools:context="com.wssholmes.stark.photos.MainActivity">

    <android.support.design.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/camera_preview"/>

    <ImageButton
        android:id="@+id/switch_camera"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_switch_camera_black_24dp"
        android:layout_gravity="top|end"
        android:layout_margin="@dimen/camera_screen_camera_switch_margin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/capture_image"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:src="@drawable/ic_camera_alt_black_24dp"
        android:layout_gravity="bottom|center"
        android:layout_marginBottom="@dimen/camera_screen_capture_button_padding"
        app:fabSize="auto"
        android:contentDescription="@string/camera_screen_fab_description"/>
</android.support.design.widget.CoordinatorLayout>
